@model IEnumerable<Google_Maps_Transport_Project.Models.Bus_details>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            busNumber
        </th>
        <th>
            bStopNumber
        </th>
        <th>
            busAddress
        </th>
        <th>
            busStopLocation
        </th>
        <th>
            latitude
        </th>
        <th>
            longitude)
        </th>
        <th>
            heading
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.busNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.bStopNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.busAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.busStopLocation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.latitude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.longitude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.heading)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
